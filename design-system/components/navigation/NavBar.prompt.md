**lfa.ch sticky top navigation.** Centered logo lockup, links left & right, outlined rose "GET STARTED" pill at the right.

```jsx
<NavBar active="What We Do" leftLinks={['About Us','What We Do','Who We Serve']} rightLinks={['Why LFA','Resources']} />
```

Composes `Button` (outline) for the CTA. Pass `active` to highlight the current item in rose.
