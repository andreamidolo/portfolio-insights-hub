**Caps-title + one-sentence value block** — the unit behind "What Makes LFA Different" and "Why LFA". Optional rose line icon.

```jsx
<FeatureCard icon={<ShieldIcon/>} title="SEC Registered">
  Authorized to provide advisory services to U.S. clients.
</FeatureCard>
```

`align="center"` for radial/grid layouts; `boxed` to wrap in a bordered white card.
